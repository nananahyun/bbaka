<style>
    /* 이미지 스타일 설정 */
    .menu-image {
        width: 100px; /* 이미지의 너비 설정 */
        height: 100px; /* 이미지의 높이 설정 */
    }

    /* 가로 정렬을 위한 스타일 설정 */
    .menu-list {
        display: flex;
        justify-content: space-between;
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .menu-item {
        flex: 1;
        text-align: center;
    }
</style>